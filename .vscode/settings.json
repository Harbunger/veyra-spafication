{
  "saveAndRunExt": {
    "commands": [
      {
        "match": ".*",
        "cmd": "brave https://demonicscans.org/game_dash.php --remote-debugging-port=9222 --user-data-dir=${workspaceFolder}/.vscode/brave-profile --load-extension=${workspaceFolder}"
      }
    ]
  }
}
